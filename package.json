{
  "name": "sass-utils",
  "browserslist": [
    "iOS >= 15"
  ],
  "scripts": {
    "build": "sass --source-map scss/main.scss css/main.css && postcss css/main.css --output css/main.css",
    "build:production": "sass --source-map scss/main.scss css/main.css && NODE_ENV=production postcss css/main.css --output css/main.css",
    "lint": "stylelint \"scss/**/*.scss\""
  },
  "devDependencies": {
    "cssnano": "^6.0.1",
    "postcss": "^8.4.31",
    "postcss-cli": "^10.1.0",
    "postcss-sort-media-queries": "^5.2.0",
    "prettier": "^3.0.3",
    "sass": "^1.69.5",
    "stylelint": "^15.11.0",
    "stylelint-config-recess-order": "^4.3.0",
    "stylelint-config-standard-scss": "^11.1.0",
    "stylelint-no-unsupported-browser-features": "^7.0.0"
  }
}
